apiVersion: v1
kind: Service
metadata:
  name: inventario-web
  namespace: inventario-itu
  labels:
    app: inventario-web
spec:
  type: NodePort
  selector:
    app: inventario-web
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
