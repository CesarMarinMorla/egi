apiVersion: v1
kind: Service
metadata:
  name: inventario-db
  namespace: inventario-itu
  labels:
    app: inventario-db
spec:
  type: ClusterIP
  selector:
    app: inventario-db
  ports:
    - name: mongo
      port: 27017
      targetPort: mongo
