name: Deploy Copy 3

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Variables de imagen
        run: |
          echo "IMAGE_WEB=test" >> "$GITHUB_ENV"
