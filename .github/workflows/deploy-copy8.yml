name: Deploy Copy 8

on:
  push:
    branches: [main]

jobs:
  deploy-core:
    runs-on: self-hosted
    steps:
      - run: echo "Core OK"

  seed-data:
    needs: deploy-core
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        working-directory: backend
        run: echo "install"
