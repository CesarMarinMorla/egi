name: Deploy Copy 11

on:
  push:
    branches: [main]

permissions:
  contents: read
  packages: write

env:
  NAMESPACE: inventario-itu
  REGISTRY: ghcr.io

jobs:
  deploy-core:
    runs-on: self-hosted
    steps:
      - run: echo "Core OK"

  seed-data:
    needs: deploy-core
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Seed SQL Server
        if: secrets.SQL_SERVER != ''
        run: echo "fake seed sql"
